vault:
  url: ${env:JWIZARD_VAULT_SERVER}
  username: ${env:JWIZARD_VAULT_USERNAME}
  password: ${env:JWIZARD_VAULT_PASSWORD}
  authentication:
    type: USERPASS

static:
  public-url: https://api.jwizard.pl/assets
