vault:
  url: http://localhost:8761
  token: ${env:ENV_VAULT_TOKEN}
  authentication:
    type: TOKEN

s3:
  host-url: http://localhost:8763
  access-key: ${vault:V_S3_USERNAME}
  secret-key: ${vault:V_S3_PASSWORD}
  root-bucket: jwizard-static
